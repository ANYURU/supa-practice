/**
 * @author Anyuru David Derrick
 * 
 *
 * @param {Number} phoneNumber 
 * @returns {Object} An object containing a message if successful or an error if unsuccessful.
 */
const sendOTPSMS = ( phoneNumber ) => {
    /**
     * Consume Africa's talking API 
     * Steps:
     * [1] Try sending the message to the user
     * [2] If the SMS is sucessfully sent return an object with a message indicating that the code was sent.
     * [3] If the msg is not successfully sent return an object with a message indicating the kind of error.
     */
    try {

    } catch (error) {

    }
}
